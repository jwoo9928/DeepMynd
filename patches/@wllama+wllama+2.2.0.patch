diff --git a/node_modules/@wllama/wllama/src/utils.ts b/node_modules/@wllama/wllama/src/utils.ts
index e0f217f..487ea3a 100644
--- a/node_modules/@wllama/wllama/src/utils.ts
+++ b/node_modules/@wllama/wllama/src/utils.ts
@@ -113,8 +113,11 @@ export const sortFileByShard = (blobs: Blob[]): void => {
 
 export const delay = (ms: number) => new Promise((r) => setTimeout(r, ms));
 
-export const absoluteUrl = (relativePath: string) =>
-  new URL(relativePath, document.baseURI).href;
+export const absoluteUrl = (relativePath: string) => {
+  // Web Worker 환경에서는 globalThis.location을 사용하고, 그렇지 않으면 document.baseURI 사용
+  const baseUri = typeof document !== "undefined" ? document.baseURI : globalThis.location.origin;
+  return new URL(relativePath, baseUri).href;
+};
 
 export const padDigits = (number: number, digits: number) => {
   return (
